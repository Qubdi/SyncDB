API Reference
=============

All public symbols are importable directly from the top-level ``syncdb`` package:

.. code-block:: python

   from syncdb import (
       Column,
       DatabaseConfig,
       FileFormat,
       FileTransfer,
       ProgressMode,
       ProgressReporter,
       SchemaMapper,
       SyncDB,
       TableSyncResult,
       TransferMode,
   )

.. toctree::
   :maxdepth: 1

   syncdb
   config
   models
   files
   schema
