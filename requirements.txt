# Minimum compatible versions for full SyncDB functionality.
# The package core imports without these; install this file when using DB connectors
# and Excel/Parquet file support.
pyodbc>=5.0
psycopg2-binary>=2.9
mysql-connector-python>=9.0
pandas>=2.0
pyarrow>=14.0
openpyxl>=3.1
