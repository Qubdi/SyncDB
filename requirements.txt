# Thin pointer — the single source of truth for dependencies is pyproject.toml.
#
#   pip install -r requirements.txt        (this file: local package + all extras)
#   pip install "Qubdi-SyncDB[all]"        (from PyPI: drivers + file formats)
#   pip install -e ".[dev]"                (development: editable + test/lint tools)
#
# requirements-dev.txt is the pinned lockfile generated from pyproject's dev
# extra via pip-compile; regenerate it there rather than editing pins here.
.[all]
